[package]
name = "rust_engine"
version = "0.1.0"
edition = "2021"
authors = ["Kosen Roman Project"]

[lib]
crate-type = ["cdylib"]

[dependencies]
# 高精度な並列処理用 (必要に応じてrayonなどを追加)
rayon = "1.8"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
